version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped

  # Optional: Postgres for local development
  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: menusaas
  #     POSTGRES_PASSWORD: password
  #     POSTGRES_DB: menusaas
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

# volumes:
#   postgres_data:
